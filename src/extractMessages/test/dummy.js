export default () => {
    const dummyFunction = () => {
        const string = __("Some dummy string");
        const string1 = __("duplicate string");
        const string3 = __("duplicate string");
    };
}
